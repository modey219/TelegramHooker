name: Build Android APK

on:
  push:
    branches: [main]
    tags: ['v*']
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 120

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install system dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq \
            git zip unzip openjdk-17-jdk-headless \
            autoconf automake libtool pkg-config \
            zlib1g-dev libncurses5-dev cmake libffi-dev libssl-dev

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Cache Buildozer
        uses: actions/cache@v4
        with:
          path: |
            ~/.buildozer
            ~/.android
          key: buildozer-v5-${{ hashFiles('buildozer.spec') }}-${{ github.run_id }}
          restore-keys: |
            buildozer-v5-${{ hashFiles('buildozer.spec') }}-
            buildozer-v5-

      - name: Install Buildozer
        run: pip install buildozer cython

      - name: Build APK
        run: |
          set +e
          buildozer android debug > build.log 2>&1
          EXIT=$?
          echo "Buildozer exit code: $EXIT"
          echo ""
          echo "=== Last 80 lines ==="
          tail -80 build.log
          echo ""
          echo "=== All .apk files ==="
          find . -name "*.apk" -exec ls -lh {} \; 2>/dev/null
          echo "=== bin/ ==="
          ls -laR bin/ 2>/dev/null || echo "(no bin/)"
          echo "=== .buildozer APK ==="
          find .buildozer -name "*.apk" -exec ls -lh {} \; 2>/dev/null || echo "(none)"
          exit $EXIT

      - name: Find APK
        id: find_apk
        run: |
          APK=$(find . -name "*.apk" -size +1M 2>/dev/null | head -1)
          if [ -z "$APK" ]; then
            APK=$(find . -name "*.apk" 2>/dev/null | head -1)
          fi
          if [ -n "$APK" ]; then
            cp "$APK" ./TelegramHooker.apk
            echo "FOUND: $APK ($(du -h TelegramHooker.apk | cut -f1))"
            echo "found=true" >> $GITHUB_OUTPUT
          else
            echo "NO APK FOUND"
            echo "found=false" >> $GITHUB_OUTPUT
            exit 1
          fi

      - name: Upload APK
        if: steps.find_apk.outputs.found == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: TelegramHooker-v${{ github.run_number }}
          path: ./TelegramHooker.apk
          retention-days: 30

      - name: Release
        if: steps.find_apk.outputs.found == 'true' && startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: ./TelegramHooker.apk

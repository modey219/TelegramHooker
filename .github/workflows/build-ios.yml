name: Build iOS IPA
on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type YES to confirm iOS build'
        required: false
jobs:
  build:
    runs-on: macos-14
    if: false
    steps:
      - run: echo "iOS build disabled - focusing on Android"
